import styled from "styled-components";

const RepoCardsCover = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  padding: 1rem;
  column-gap: 1rem;
  gap: 1rem;
  justify-items: center;
`;

export { RepoCardsCover };
